registration:
  server:
    hostname: localhost

spring:
  config:
    name: discovery-server


eureka:
  server:
    enableSelfPreservation: false
  instance:
    hostname: localhost
  client: 
    registerWithEureka: false
    fetchRegistry: false

server:
  port: 1111  
